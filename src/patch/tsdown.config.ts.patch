diff --git a/apps/desktop/tsdown.config.ts b/apps/desktop/tsdown.config.ts
index d34cf1bf26..38e0ff04e1 100644
--- a/apps/desktop/tsdown.config.ts
+++ b/apps/desktop/tsdown.config.ts
@@ -10,7 +10,9 @@ export default defineConfig([
     fixedExtension: false,
     dts: false,
     clean: false,
-    deps: { neverBundle: ['electron'] },
+    // Local customization: the injected feature layer stays a separate runtime file, so only the
+    // entry that imports it declares it (the preload entries must stay self-contained).
+    deps: { neverBundle: ['electron', '../local/features/profile-recovery.mjs', '../local/features/diagnostics.mjs', '../local/features/diagnostics-log.mjs'] },
   },
   ...(['preload', 'preload-app'] as const).map(name => ({
     // Sandboxed Electron preloads run as CommonJS even though the application package is ESM.
